import 'package:flutter/material.dart';

/// Barbershop palette: warm ink base, brass as the working accent,
/// barber-pole red reserved for status and the signature stripe.
class AppColors {
  AppColors._();

  static const ink = Color(0xFF14110D);
  static const surface = Color(0xFF1F1A13);
  static const surfaceHigh = Color(0xFF2B2317);
  static const brass = Color(0xFFC9A24B);
  static const brassDim = Color(0xFF8A7238);
  static const barberRed = Color(0xFF9C3B33);
  static const cream = Color(0xFFF1E7D6);
  static const steel = Color(0xFF938872);
  static const divider = Color(0xFF362C1E);
}
